%modules = (
    "QtAddOnSerialPort" => "$basedir/src/serialport",
);

%moduleheaders = (
);

%classnames = (
);

%mastercontent = (
);

%modulepris = (
);

%dependencies = (
        "qtbase" => "refs/heads/master",
);
